# 测试接缝

**这个站**只有两个测试接缝。加断言时先想清楚属于哪一个 —— 不要新建第三个。

| | 接缝 | 跑在哪 | 管什么 |
|---|---|---|---|
| ① | **构建产物**（`export/`，Vitest） | `out/`，无浏览器 | 导出了哪些文件、文件里写了什么、总共多大 |
| ② | **浏览器**（Playwright + Lighthouse CI，见 #66） | Cloudflare Pages 预览链接 | 只放**运行时才测得出**的：Core Web Vitals、reduced-motion、幕布转场不搞坏导航 |

**动效手感不进自动化。** 滚动惯性、逐行揭示的节奏、遮罩视差的速度只能真机看 —— 写断言说「视差感觉对」是纯浪费。

## 站以外：小帐本（`moneybook/`，#98）

`public/app/moneybook/` 是一个自带状态与算术的离线 app，不是这个站的页面。它有**自己的一个**接缝，与上面两条并列而不是第三条站内接缝：

| | 接缝 | 跑在哪 | 管什么 |
|---|---|---|---|
| ⓐ | **ledger 核心**（`moneybook/`，Vitest） | 纯函数，无 DOM、无 `localStorage` | 迁移、两侧独立、转帐、汇率、预算、固定收支补记 |

它之所以测得起来，是因为 `ledger.js` 不碰 DOM 也不碰储存 —— 载入用的原始字符串由外层传进来。断言只打在「给定一份状态与一串操作，得到什么新状态、什么派生数字」上，与接缝 ① 的「拿产物去对产物」同一个口径：都不 mock 内部。

**渲染与切换器的 DOM 行为不在这里。** 它们的错误一眼可见，写起来脆且贵，用浏览器手测覆盖。

## 怎么跑

```bash
npm test        # 构建 + 断言（构建失败则整条命令失败）
npm run test:only   # 只跑断言，复用现有的 out/
npm run test:watch  # 改断言时用
```

## 怎么加一条断言

1. **先找现成的文件。** `export/assets.test.ts` 管资源，`export/hygiene.test.ts` 管「不该出现的东西」。
2. **`hygiene` 里加一条只需在 `FORBIDDEN` 数组加一行** —— 写上 `what` / `match` / `why`，`why` 要说清为什么，未来的人才知道能不能删。
3. **需要新数据时**，在 `helpers/export.ts` 里加一个派生字段，别改它的结构。
4. **新关注点开新文件**，放 `export/` 下。fixture 会被自动复用。

`helpers/export.ts` 只读文本文件（html / css / xml / txt / json / js）。`out/` 里有几百 MB 图片，**绝不要整包读进内存** —— 用 `sizes` 拿体积，用 `has()` 判存在。

## 两条特殊约定

**棘轮（ratchet）** —— 有些断言在对应的票做完之前不可能为 0。这类写成「不超过基线」，基线只允许降不允许升，并在注释里写明哪张票会把它清零。清零之后就把注释改成硬约束，别留着「基线」字样误导人。

> 用过一次：冗余原图的基线从 46 起步，#71 做完后归 0。中间那段时间它挡住了「又提交一张原图」的回退。

**已知缺失清单** —— 只放**已经有票在跟**的缺失项，且**必须配一条反向断言**：修好了却没从清单里删，测试同样会红。不要用它掩盖新问题。

> 这个约定已经救过一次：#81 把 `css/` 与 `js/` 移进 `public/` 之后，反向断言立刻报「这些已经不缺了，请从清单里删除」。没有它，那份清单会一直挂着，掩盖后续真正的缺失。
>
> 当前在用的一份：`export/motion.test.ts` 的 `KNOWN_MISSING` 里挂着 `404.html`（Next 的内建 404 不经过 `Shell`，拿不到动效外壳），由 #93 跟进，反向断言已配好。

## 各票会往这里加什么

| 票 | 断言 |
|---|---|
| ~~#71 资产瘦身~~ | ✅ 已完成 —— `budget.test.ts`（总体积 / 单文件 25MiB / 文件数 20,000）；冗余原图棘轮已归 0 |
| ~~#73 URL 形态~~ | ✅ 已完成 —— `urls.test.ts`（扁平导出、sitemap 覆盖、canonical 形态、`_redirects`、**全站站内链接死链检查**） |
| ~~#74 补核心页~~ | ✅ 已完成 —— `geo.test.ts`（核心页的「快速答案」块、结构化数据、FAQ 可见性） |
| ~~#82 内容页 FAQ 不可见~~ | ✅ 已完成 —— FAQ 可见性断言（问题**与**答案）已放开到全站每一个 HTML 页面；16 个内容页的 74 条问答改为可见渲染，`FAQPage` 由 `split-content-lang.mjs` 从那段可见问答生成 |
| #75 双语路由 | 语言配对一一对应无孤儿；页面语言纯净 |
| #76 内容页拆分 | 内容页的语言配对与语言纯净 |
| #77 sitemap/llms | hreflang 双向配对；条目数与实际页面数一致，无死链 |
| ~~#78 中文字体~~ | ✅ 已完成 —— `fonts.test.ts`（三个子集的体积区间、总体积、OFL 三份、页脚链接、`@font-face` 与 unicode-range、只预加载正文字重） |
| ~~#81 css/js 缺失~~ | ✅ 已完成 —— `css/` 与 `js/` 移进 `public/`，允许清单已清空 |
| ~~#87 逐行揭示~~ | ✅ 已完成 —— `motion.test.ts`（动画库不进首屏脚本、揭示预备态脚本早于 `<body>`） |
| ~~#89 幕布转场~~ | ✅ 已完成 —— `motion.test.ts`（每页都注入动效外壳、外壳与 Lenis 在产物里）；`404.html` 进了已知缺失清单，#93 在跟，配了反向断言 |
| ~~#88 遮罩视差~~ | ✅ 已完成 —— `motion.test.ts`（视差只出现在案例缩略图与创始人照片所在的页，每页有上限） |
| ~~#98 小帐本跨境记帐~~ | ✅ 已完成 —— `moneybook/ledger.test.ts`（v1→v2 迁移与畸形资料救援、两侧独立、转帐两侧同动、汇率派生、家用、固定收支分侧补记、**单币种回归**、预算分侧）；`app/**` 已加入收录排除清单与 `robots.txt` 的 Disallow |
| #92 性能预算 | 首屏 JS < 200KB、首屏总重 < 800KB（不含视频）；破线即失败 |
