-- 顺手把当时的价钱也记下来。
--
-- 起因是「能不能排除 business class 的位子」。不登录看不到座位分类，
-- 但价钱是看得到的 —— 实测 Kluang→JB 的 business class 通常 >RM30，
-- 而四个礼拜日 8 班车的价钱全部落在 RM14–28。
--
-- 目前的证据指向「剩 4 个不是 business，是 OKU」：23:25 那班剩 4 位时
-- 价钱 RM20（全场最低），有 77 位时反而 RM27 —— 位子越少价钱越低，
-- 跟「只剩贵位」的假设方向相反。
--
-- 但那只是四天的快照。记下来以后要判断就有真数据，不用重新收集。
-- **写入量不变**：仍然只在座位数变了才写一行，价钱只是搭便车存进那一行。
ALTER TABLE seat_log ADD COLUMN fare TEXT;
