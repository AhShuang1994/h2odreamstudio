# H2ODreamer Studio

阿爽（Hui Huang Ong）的网站设计工作室官网。Next.js 静态导出，部署在 Cloudflare Pages。

## Agent skills

### Issue tracker

GitHub Issues（`AhShuang1994/h2odreamstudio`），通过 `gh` CLI 操作。See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context —— 根目录 `CONTEXT.md` + `docs/adr/`。See `docs/agents/domain.md`.
