node_modules/
docs/

# Keep local Claude Code state out of the repo, but track the skills symlinks
# so installed skills survive a fresh clone.
.claude/*
!.claude/skills/

# Xiaohongshu (小红书) post working images — deliverables for the XHS
# platform, not referenced by the website
assets/blog/xiaohongshu/

# Raw Gemini PNG sources for blog illustrations (converted to .webp; keep local, do not commit)
assets/blog/*.png

# Raw PNG sources for demo mockup photos (converted to .webp; keep local, do not commit)
assets/demos/**/*.png

# 看图复刻的产物 —— 默认不进 git、不上线（screenshot-to-web 技能）
_replicas/
